# dynus_setup


1. Run bash script
2. Set VEH_NAME variable in bashrc to unique vehicle name
3. Set MAV_SYS_ID variable in bashrc to unique mavlink id
4. To try the trajectory generator with mocap run ~/code/mavros_ws/src/ros2_px4_stack/scripts/mocap_trajgen_tmux.py 


